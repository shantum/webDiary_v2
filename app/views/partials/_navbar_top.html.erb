<nav class="navbar navbar-light bg-faded">
  <a class="navbar-brand" href="/">webDiary</a>
  <ul class="nav navbar-nav">

    <li class="nav-item active">
      <!--link for modal triggered through javascript-->
      <a class="nav-link nav-bar-new-link" href="">Add new link</a>
      <!--modal body-->
      <div class="modal fade nav-bar-new-link-modal">
        <div class="modal-dialog" role="document">
          <div class="modal-content nav-bar-new-link-modal-content">
            <div class="modal-header">
              <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                <span aria-hidden="true">&times;</span>
              </button>
              <h4 class="modal-title">Add a new link</h4>
            </div>
            <div class="modal-body">
              <%= render 'partials/new_link_form'%>
            </div>

          </div><!-- /.modal-content -->
        </div><!-- /.modal-dialog -->
      </div><!-- /.modal -->
    </li>

    <li class="nav-item">
      <a class="nav-link" href="/user_links/index">View all links</a>
    </li>
  </ul>

  <form class="form-inline float-xs-right">
    <input class="form-control" type="text" placeholder="Search">
    <button class="btn btn-outline-success" type="submit">Search</button>
  </form>

  <ul class="nav navbar-nav">
    <li class="nav-item dropdown float-xs-right">
      <% if user_signed_in? %>
          <a class="nav-link dropdown-toggle nav-user-dropdown-link" href="http://example.com" id="supportedContentDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Logged in as: <%= current_user.email %></a>
          <div class="dropdown-menu nav-user-dropdown-menu" aria-labelledby="supportedContentDropdown">
            <%= link_to 'Edit profile', edit_user_registration_path, :class => 'dropdown-item' %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'dropdown-item'  %>
          </div>
      <% else %>
          <a class="nav-link dropdown-toggle" href="http://example.com" id="supportedContentDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Create a new account!</a>
          <div class="dropdown-menu" aria-labelledby="supportedContentDropdown">
            <%= link_to "Sign up", new_user_registration_path, :class => 'dropdown-item'  %>
            <%= link_to "Login", new_user_session_path, :class => 'dropdown-item'  %>
          </div>
      <% end %>
    </li>
  </ul>

</nav>

<%= render 'partials/category_tabs' %>