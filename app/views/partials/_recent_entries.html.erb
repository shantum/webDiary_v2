<h3>Latest Entries:</h3>
<br>
  <div class="ui cards">
  <% current_user_recent_links.each do |user_link| %>
        <%= (render 'partials/link_card', user_link: user_link) unless user_link.nil? %>
  <%end%>
  </div>
